# write program to print area of triangle
base=float(input('enter base of triangle: '))
height=float(input('enter height of triangle: '))
area= ((base*height)*(0.5))
print(f'the area of triangle is {area}')